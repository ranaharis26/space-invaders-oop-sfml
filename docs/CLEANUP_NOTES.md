# Cleanup Notes

This repository-ready version intentionally excludes heavy/generated files from the original ZIP:

- `.vs/` Visual Studio cache files
- `.exe` binaries
- `.dll` files
- build/output folders
- patch/helper scripts
- assignment PDF
- local runtime leaderboard file

These files are not needed in a clean public GitHub repository. The code, build files, README, .gitignore, and screenshot folder are included.
